class Employee:
    __slots__ = ['name', 'age', 'salary']#Define allowed attributes to save memory and fasten attribute access.
    
    def __init__(self, name, age,  salary):#Instance attributes OF the class Employee
        self.name = name
        self.age = age
        self.salary = salary

    
    def increase_salary(self, percentage):
        self.salary += self.salary * (percentage / 100)

class SlotsInspectorMixin:
    def has_slots(self):
        return hasattr(self, '__slots__')



class Developer(Employee, SlotsInspectorMixin): #Inheritance: Developer class inherits from Employee class
    __slots__ = ['framework']#Define allowed attributes to save memory and fasten attribute access.
    
    def __init__(self, name, age, salary, framework):
        super().__init__(name, age, salary)
        self.framework = framework



    def increase_salary(self, percentage, bonus=0): #Method overriding: Custom increase_salary method for Developer class
        super().increase_salary(percentage)
        self.salary += bonus
        


employee2 = Developer("Bob", 45, 95000 , "Django")



employee2.increase_salary(10, 30)  # Using overridden method
print(employee2.salary)  # Display Bob's salary
print(employee2.framework)  # Display Bob's framework
print(employee2.name)  # Display Alice's name
print(employee2.has_slots())  # Check if employee2 has __slots__ defined



   
    
